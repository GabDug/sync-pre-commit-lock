---
version: 2
updates:

  # Maintain SHA-pinned GitHub Actions (Dependabot updates the commit SHA
  # and trailing version comment together when a new release is available).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"
